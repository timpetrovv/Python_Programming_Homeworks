# From math library import the number pi.
from math import pi

# User Input the radiant number
radiant = float(input())

# Calculate the number of degrees.
degrees = radiant * 180 / pi

# Print the degrees:
print(degrees)